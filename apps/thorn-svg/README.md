# thorn (CLI)

What the profile promises, testable without a screen:

```
thorn check drawing.svg          # hold it to the profile; exit 1 on a finding
thorn shapes drawing.svg         # list its shapes in paint order
thorn render drawing.svg -o out.png [--scale 2]   # through resvg
```
